# {{AREA_TITLE}} — API Contract

Area's stable public surface: exported names/signatures, error variants, config fields, feature flags.

Spec, not documentation of what happens to exist. Any change here is normative → goes through gate 1 (consumers depend on this).

No ID series of its own: every table row carries a `Req` column naming the owning requirement ID(s), enumerated never ranged; `—` plus a raised gap when none (`docs/specs/README.md` rule 7).

---

## Types

*(Empty.)*

## Functions

*(Empty.)*

## Errors

*(Empty. Every failure mode is a distinct variant — never stringly-typed.)*

## Configuration

*(Empty. Field name, type, default, and the requirement ID governing its effect.)*

## Feature flags

*(Empty. Flag name, what it gates, default-on or not.)*
